<%= form_with(model: importacao, local: true) do |form| %>
  <% if importacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(importacao.errors.count, "error") %> prohibited this importacao from being saved:</h2>

      <ul>
        <% importacao.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :arquivo %>
    <%= form.file_field :arquivo %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
